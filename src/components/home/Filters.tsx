export default function Filters() {
  return (
    <div className="flex flex-row w-full h-full items-center justify-between">
      <p className="text-lg lg:text-2xl">Filters</p>
      <select>
        <option value="all">All</option>
      </select>
      <select>
        <option value="all">All</option>
      </select>
    </div>
  );
}
